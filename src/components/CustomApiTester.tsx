import React from 'react';
import { CustomApiSelector } from './CustomApiSelector';

export const CustomApiTester: React.FC = () => {
    return (
        <>
            <CustomApiSelector />
            {/* Custom API Tester content will be added here */}
        </>
    );
};
